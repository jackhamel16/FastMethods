import numpy as np
import numpy.linalg as lg
import interaction
import utilities as utils
import scipy.special as funcs


for i in range(Q):
    print(M2L_list[0][55][i], M2L_list[55][0][i])
#s1 = src_list[0]
#s2 = src_list[1]
#
#dist = np.sqrt((s1.x-s2.x)**2 + (s1.y - s2.y)**2)
#test_pot = funcs.hankel1(0, k * dist)
#
#G = interactions.build_G([0,1], [0,1], k)

